# Ver.6.3

- 非チェーンのGoogle初期補正を最大±2点の連続値で扱い、最終スコアを0.1点単位で表示
- 「今営業中の店だけ」をデフォルトON
- ラーメン種類「おまかせ」を「すべて」に変更
- Chain Prior / 非チェーン初期推定 / Google補正の説明UIを削除
- 店舗カードのメタ情報をユーザーフィードバック件数と信頼度へ簡素化
- フッターのスコア式表示を削除
- データが少ない間はGoogle Maps評価等で暫定算出し、フィードバック蓄積で精度が上がる旨を小さく表示
- 地図を選択検索半径へfitBoundsするよう変更
